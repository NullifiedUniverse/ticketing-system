# 超簡單！新手保姆級安裝教學 🍼

想用這套系統但看到一堆英文就頭痛？不知道什麼是 Node.js？**看這篇就對了。**

請**完全按照步驟**操作，不要跳過任何一步。

---

## 第一階段：下載必要軟體
在開始之前，你的電腦需要安裝兩個東西。

1.  **下載 Node.js (LTS 版本)**
    *   網址：[https://nodejs.org/](https://nodejs.org/)
    *   點擊左邊綠色的按鈕 **"LTS"** (長期支援版)。
    *   下載後像安裝一般軟體一樣，一直按 "Next" (下一步) 直到完成。
2.  **下載 Git (選擇性，但建議安裝)**
    *   網址：[https://git-scm.com/downloads](https://git-scm.com/downloads)
    *   下載適合你電腦的版本並安裝。

---

## 第二階段：搞定資料庫 (Firebase)
這是最難的一步，深呼吸，跟著做。

1.  前往 [https://console.firebase.google.com/](https://console.firebase.google.com/) 並登入你的 Google 帳號。
2.  點擊 **"建立專案" (Create a project)**。
    *   專案名稱隨便取：`MyTicketSystem`。
    *   Google Analytics (分析)：可以**關閉** (我們不需要)。
    *   點擊 **建立專案**。
3.  **設定資料庫：**
    *   專案建立好後，點擊左側選單的 **建構 (Build)** -> **Firestore Database**。
    *   點擊 **建立資料庫 (Create Database)**。
    *   **重要：** 選擇 **測試模式 (Test Mode)** (不要選鎖定模式)。
    *   點擊 **啟用 (Enable)**。
4.  **下載鑰匙 (密鑰檔案)：**
    *   點擊左上角「專案總覽」旁邊的 **齒輪圖示 ⚙️** -> **專案設定 (Project Settings)**。
    *   切換到 **服務帳戶 (Service accounts)** 分頁。
    *   點擊下方按鈕 **產生新的私鑰 (Generate new private key)** -> **產生金鑰**。
    *   電腦會下載一個檔案 (檔名很長，例如 `myticketsystem-firebase...json`)。
    *   **把這個檔案收好！** 等一下第四階段會用到。

---

## 第三階段：下載程式碼
1.  下載本專案的程式碼 (在 GitHub 點擊 **Code** -> **Download ZIP**，或使用 Git Clone)。
2.  解壓縮到一個資料夾 (例如：`C:\TicketSystem` 或桌面)。

---

## 第四階段：放入鑰匙
1.  打開你剛剛解壓縮的資料夾。
2.  進入 `backend` 資料夾。
3.  找到你在第二階段下載的那個 **很長的 .json 檔案**。
4.  **重新命名** 該檔案為：`serviceAccountKey.json` (注意大小寫)。
5.  將改名後的檔案 **放入** `backend` 資料夾中。
    *   *檢查：* 確認你的 `backend` 資料夾裡現在有一個 `serviceAccountKey.json`。

---

## 第五階段：設定檔案 (.env)
1.  一樣在 `backend` 資料夾中，右鍵 -> 新增 -> 文字文件。
2.  將檔名改為 `.env` (如果 Windows 警告，就改成 `.env.` 它會自動修正)。
3.  用記事本打開它。
4.  複製以下內容並貼上：
    ```env
    PORT=3001
    NODE_ENV=development
    JWT_SECRET=supersecretpassword123
    API_KEY=admin123
    
    # Email 設定 (如果是測試可以先不填)
    SMTP_HOST=smtp.gmail.com
    SMTP_PORT=465
    SMTP_SECURE=true
    SMTP_USER=your-email@gmail.com
    SMTP_PASS=your-app-password
    ```
5.  儲存並關閉。

---

## 第六階段：啟動系統 🚀
最後一步了！

1.  **打開終端機 (Terminal / Command Prompt)：**
    *   **Windows:** 在 `TicketSystem` 資料夾內按右鍵 -> "Open in Terminal" (或是點擊上方網址列輸入 `cmd` 按 Enter)。
    *   **Mac:** 打開 Terminal 並 `cd` 到該資料夾。

2.  **安裝並啟動後端 (Backend)：**
    *   輸入以下指令 (每一行按一次 Enter)：
        ```bash
        cd backend
        npm install
        npm start
        ```
    *   如果你看到 "Server running on port 3001"，**恭喜！成功了一半！**
    *   **不要關閉這個視窗**。

3.  **安裝並啟動前端 (Frontend)：**
    *   打開一個**新的**終端機視窗 (原本那個留著)。
    *   一樣回到專案資料夾。
    *   輸入：
        ```bash
        cd frontend
        npm install
        npm start
        ```
    *   瀏覽器應該會自動跳出 `http://localhost:3000`。

**完成了！享受你的票務系統吧！** 🎉
